export function createSound(){let context:AudioContext|undefined;return {play(outcome:string,station=0){try{context??=new AudioContext();void context.resume();const t=context.currentTime;const frequencies=outcome==='success'?[523,659,784]:outcome==='failed'?[220,165]:[330,440];frequencies.forEach((f,i)=>{const osc=context!.createOscillator(),gain=context!.createGain(),pan=context!.createStereoPanner();osc.type='sine';osc.frequency.value=f;pan.pan.value=(station-1.5)/3;gain.gain.setValueAtTime(0,t+i*.09);gain.gain.linearRampToValueAtTime(.055,t+i*.09+.012);gain.gain.exponentialRampToValueAtTime(.001,t+i*.09+.2);osc.connect(gain);gain.connect(pan);pan.connect(context!.destination);osc.start(t+i*.09);osc.stop(t+i*.09+.22);});}catch{}},close(){void context?.close();}};}
